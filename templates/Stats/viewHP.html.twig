{% extends 'base.html.twig' %}

        {% block body %}

            {% for message in app.flashes("success") %}
                <div class="alert alert-success" role="alert">{{ message }}</div>
            {% endfor %}

            {% for message in app.flashes("error") %}
                <div class="alert alert-danger" role="alert">{{ message }}</div>
            {% endfor %}
            <hr>

            <p style="color: gold;">Punkty życia w twojej wiosce : ({{ value }})</p>


            <h2>Czy chcesz zwiększyć poziom życia w twojej wiosce ?</h2>

            <P style="color: gold">Wymagane : {{ required }} Złota</P>
            <P style="color: gold">Wymagane : {{ required }} Węgla</P>


            <a href="{{ path('hpAdd') }}"><button class="btn-success">TAK</button></a>
            <a href="{{ path('viewMenu') }}"><button class="btn-danger">NIE</button></a>

            </br>
            </br>
            <a href="{{ path('myProfilVillage', {'id': app.user.id}) }}"><button class="btn-default">Wróc</button></a>





        {% endblock %}