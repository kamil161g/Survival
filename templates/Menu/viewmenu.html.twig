{% extends 'base.html.twig' %}
    {% block body %}

        <div class="container" style="border: solid 1px black; height: 80%">

            <button class="btn btn-default" style="width: 10%" type="submit">Mój profil</button><br/>
        {% if  village %}
            <a href="{{ path('myProfilVillage', {'id': app.user.id}) }}">
                <button class="btn btn-default" style="width: 10%" type="submit">Moja wioska</button><br/>
            </a>
            <button class="btn btn-default" style="width: 10%" type="submit">Polowanie</button><br/>
            <button class="btn btn-default" style="width: 10%" type="submit">Wyprawa</button><br/>
        {% else %}
            <button class="btn btn-default" style="width: 10%" type="submit">Stwórz wioskę</button><br/>
        {% endif %}
        </div>

    {% endblock %}